{
  "name": "static-website-template",
  "version": "1.0.0",
  "description": "template for static website",
  "author": "Vincent Will <vincent.will@vaitrade.de>",
  "repository": {
    "type": "git",
    "url": "https://github.com/Vincenius/static-website-template"
  },
  "scripts": {
    "clean": "rimraf dist/{css/*,js/*,images/*}",
    "autoprefixer": "postcss -u autoprefixer -r dist/css/*",
    "scss": "node-sass --output-style compressed -o dist/css src/scss",
    "lint-scss": "stylelint src/scss/*.scss --syntax scss || true",
    "babel": "mkdirp dist/js -p && npx babel src/js --out-file dist/js/main.min.js --presets minify --source-maps",
    "imagemin": "imagemin src/images/**/* --out-dir=dist/images",
    "serve": "browser-sync start --server --files \"dist/css/*.css, dist/js/*.js, **/*.html, !node_modules/**/*.html\"",
    "build:css": "run-s lint-scss scss autoprefixer",
    "build:js": "run-s babel",
    "build:images": "run-s imagemin",
    "build": "run-s build:*",
    "watch:css": "onchange \"src/scss\" -- run-s build:css",
    "watch:js": "onchange \"src/js\" -- run-s build:js",
    "watch:images": "onchange \"src/images\" -- run-s build:images",
    "watch": "run-p serve watch:*"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "autoprefixer": "^9.0.1",
    "babel-preset-minify": "^0.5.1",
    "browser-sync": "^2.26.5",
    "eslint": "^6.5.1",
    "imagemin-cli": "^5.0.0",
    "mkdirp": "^0.5.1",
    "node-sass": "^4.12.0",
    "npm-run-all": "^4.1.3",
    "onchange": "^4.1.0",
    "postcss-cli": "^6.0.0",
    "rimraf": "^2.5.4",
    "stylelint": "^9.4.0"
  },
  "main": ".eslintrc.js"
}
